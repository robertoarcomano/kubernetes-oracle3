apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: rundeck
  name: rundeck
  namespace: automation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rundeck
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: rundeck
    spec:
      containers:
      - image: homelabs/rundeck
        name: rundeck
        ports:
        - containerPort: 4440
        volumeMounts:
          - name: "rundeck1"
            mountPath: "/var/lib/rundeck1"
        resources: {}
      volumes:
        - name: "rundeck1"
          hostPath:
            path: /var/lib/pv/rundeck
status: {}
