apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: jenkins
  name: jenkins
  namespace: devops
spec:
  ports:
  - port: 8080
    name: jenkins
    protocol: TCP
    targetPort: 8080
    nodePort: 32100
  - port: 50000
    name: jenkins-log
    protocol: TCP
    targetPort: 50000
  selector:
    app: jenkins
  type: NodePort
status:
  loadBalancer: {}
